---
name: create-component
description: React コンポーネントを作成する
---

## 手順

1. コンポーネント名を確認する
2. `src/components/` 配下にディレクトリを作成する
3. 以下のファイルを生成する:
   - `index.tsx` — コンポーネント本体
   - `index.test.tsx` — テストファイル

## ルール

- 関数コンポーネントを使用する
- Props は `interface` で定義する
- デフォルトエクスポートは使用しない
